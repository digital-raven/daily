#!/usr/bin/make -f
#export DH_VERBOSE=1
export PYBUILD_NAME=daily
export PYBUILD_INSTALL_ARGS=--install-lib=/usr/share/daily/ --install-scripts=/usr/share/daily/

%:
	dh @$ --with python3 --buildsystem=pybuild
